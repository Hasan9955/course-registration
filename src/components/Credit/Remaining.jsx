
const Remaining = ({remaining}) => {
    return (
        <div>
            <p className="text-lg font-bold text-blue-500">Credit Hour Remaining {remaining} hr</p>
            <hr className="my-3" />
        </div>
    );
};

export default Remaining;