/* eslint-disable react/prop-types */


const Cart = ({data}) => {
    
    return (
        <div>
            <p className="text-lg font-bold mb-3">{data.title}</p>
        </div>
    );
};

export default Cart;